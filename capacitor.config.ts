import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'kafka_stream_angular_part',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
